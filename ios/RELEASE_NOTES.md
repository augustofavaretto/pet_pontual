# iOS - versão 1.0.0

- Projeto configurado com `CFBundleShortVersionString` 1.0.0 e build `1` seguindo o `pubspec.yaml`.
- Assinatura permanece com perfil de desenvolvimento; ajuste o *bundle identifier* e certificados antes de subir à App Store.
- Use este arquivo para documentar mudanças específicas da plataforma em releases futuros.
